export namespace Models {
  export interface UserAuth {
    email: string;
    password: string;
  }

  export interface SessionOpt {
    code: number;
    sessionId: string;
  }

  export interface UserAuth {
    email: string;
    password: string;
  }
}
